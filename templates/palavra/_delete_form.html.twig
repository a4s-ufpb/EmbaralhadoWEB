<form method="post" action="{{ path('app_palavra_delete', {'id': perguntum.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ perguntum.id) }}">
    <button class="btn">Delete</button>
</form>
